[pytest]
asyncio_mode = auto
markers =
    ai_v2: Canonical AI Chat v2 tests (`app/tests/*`).
    legacy_ai: Legacy AI v1 tests (`tests/*`) for analysis/gateway compatibility and remaining v1 AI surfaces.
